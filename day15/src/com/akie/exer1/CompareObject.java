package com.akie.exer1;
/*
* interface CompareObject
* public int compareTo(Object o)
* //若返回值是0,代表相等;若为正数代表,当前对象大；负数代表当前对象小
}
*
* */
public interface CompareObject {
    //若返回值是0,代表相等;若为正数代表,当前对象大；负数代表当前对象小
    public int compareTo(Object o);
}
